language: python
python:
    - "3.5"
install: 
    - "pip install -r requirements.txt --use-mirrors"
    - "pip install coverage"
    - "pip install coveralls"
script: 
    - "coverage run --source=tests pytest tests"
after_success:
    coveralls
